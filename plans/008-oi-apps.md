# Plan 008: Build OI Base, Workflow, Workflow Advance, Delegation, Login-As Apps

> Executor instructions: implement installable Go ERP apps with feature parity against local OI source inventory. Do not copy OI code/assets unless a written license/provenance decision explicitly allows it.
>
> Drift check: run `git diff --stat -- addons/oi_* internal/workflow internal/delegation internal/impersonation frontend/packages/oi-*`.

## Status

- Priority: P2
- Effort: L
- Risk: HIGH
- Depends on: `plans/002-base-kernel-orm-modules.md`, `plans/003-security-users-acl-rules.md`, `plans/004-web-owl-theme.md`, `plans/005-automation-mail-scheduler.md`
- Category: migration
- Planned at: no commits, 2026-06-16
- Status: DONE
- Completed at: 2026-06-16
- Verification: `go test ./internal/workflow ./internal/delegation ./internal/impersonation ./addons/oi_base ./addons/oi_workflow ./addons/oi_workflow_advance ./addons/oi_delegation ./addons/oi_login_as`; `go test ./...`; `go test -race ./...`; `go vet ./...`; `pnpm -C frontend test -- oi`; `make ci`
- Inventory report: `reports/oi_inventory.json`

## Why This Matters

The user requires `oi_workflow`, `oi_workflow_advance`, `oi_delegation`, and `oi_login_as` as installable base apps in the new ERP. Local OI source exists for Odoo 18 and 17, but not Odoo 19. The correct target is a feature-parity port into the Go ERP module system.

## Current State

Primary source paths:
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_base`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_workflow`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_workflow_advance`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_delegation`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_login_as`

Comparison source paths:
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons/oi_base`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons/oi_workflow`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons/oi_workflow_advance`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons/oi_delegation`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons/oi_login_as`

Odoo 18 local inventory:
- `oi_base`: 18 source files, 699 lines across py/xml/csv/js/scss-like counted files
- `oi_workflow`: 63 source files, 3964 lines
- `oi_workflow_advance`: 32 source files, 1565 lines
- `oi_delegation`: 29 source files, 1024 lines
- `oi_login_as`: 16 source files, 299 lines

Manifest facts:
- OI modules are OPL-1.
- `oi_workflow` 18.0.1.8.7 depends on `mail`, `oi_base`, `oi_base_cache`, `web`, `base_automation`, `oi_web_selection_field_dynamic`, `oi_web_selection_tags`, and Python package `humanize`.
- `oi_workflow_advance` 18.0.1.4.25 depends on `oi_workflow`, `oi_web_flowchart`.
- `oi_delegation` 18.0.1.0.14 depends on `base`, `hr`, `mail`, `oi_base`, `oi_workflow`.
- `oi_login_as` 18.0.1.2.7 depends on `web`, `portal` and is auto-install.

Runtime dependency inventory:
- Python/Odoo source uses `humanize`, `dateutil.relativedelta`, `lxml`, `markupsafe`, and `pytz`.
- Frontend depends on OWL/web registries, form/list controller patches, systray, and debug menu hooks.
- Replace Python `safe_eval`/dynamic code with CEL, Starlark, or a purpose-built sandboxed policy DSL.
- Replace dynamic XML view mutation with declarative UI metadata generated by the workflow engine.

## Commands You Will Need

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| OI tests | `go test ./internal/workflow ./internal/delegation ./internal/impersonation ./addons/oi_base ./addons/oi_workflow ./addons/oi_workflow_advance ./addons/oi_delegation ./addons/oi_login_as` | exit 0 |
| All tests | `go test ./...` | exit 0 |
| Frontend tests | `pnpm -C frontend test -- oi` | exit 0 |
| Inventory | `find /Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons/oi_workflow -type f | sort` | shows source files |

## Scope

In scope:
- `addons/oi_base`
- `addons/oi_workflow`
- `addons/oi_workflow_advance`
- `addons/oi_delegation`
- `addons/oi_login_as`
- `internal/workflow`
- `internal/delegation`
- `internal/impersonation`
- `frontend/packages/oi-workflow`
- `frontend/packages/oi-flowchart`
- `frontend/packages/oi-login-as`

Out of scope:
- OI mobile modules
- standard document workflow plugins such as purchase/sale/expense unless added later
- exact static description images/gifs/logos
- direct Python execution

## Steps

### Step 1: Build `oi_base`

Feature inventory from local source:
- base model utilities
- action helpers
- qweb/helpers
- sequence helpers
- group settings
- attachment mixin
- XML helpers
- national ID helper
- health endpoint equivalent to `/ping`
- group hierarchy helpers

Implement Go equivalents only where needed by OI modules.

Verify: `go test ./addons/oi_base ./internal/base -run TestOIBase` exits 0.

### Step 2: Build workflow core models

From `oi_workflow/models` and data/views inventory, implement:
- approval configuration
- approval settings
- approval states
- approval records
- approval buttons
- approval automation
- approval escalation
- approval logs
- approval log voting
- approval forward records
- cancellation records
- state tags
- mail template/activity integration
- user/group extensions
- model activation flags
- sequence mixin behavior
- committee/voting support
- mass approve
- dynamic form/list view metadata injection

Verify: `go test ./internal/workflow ./addons/oi_workflow -run TestWorkflowCore` exits 0.

### Step 3: Build workflow process actions and wizards

Implement:
- approve
- reject
- return
- forward
- transfer
- cancel
- cancel workflow
- draft reset
- server action execution
- email action
- registered method action
- state update
- process wizard
- expression editor metadata

Rules:
- all transitions enforce ACL and record rules
- no arbitrary Python script execution; use safe expression/action DSL
- every transition writes an audit log

Verify: `go test ./addons/oi_workflow -run TestWorkflowTransitions` exits 0.

### Step 4: Build workflow UI

Implement:
- workflow settings views
- approval buttons
- approval log views
- state tags
- model expression editor UI
- workflow status widgets
- backend assets

Use original UI components. Do not copy images/gifs/logos from OI static descriptions.

Verify: `pnpm -C frontend test -- oi-workflow` exits 0.

### Step 5: Build workflow advance

From `oi_workflow_advance` inventory, implement:
- workflow model
- workflow node
- workflow transition
- workflow node action
- workflow process
- approval log integration
- graph/flowchart UI
- process wizard
- transition rules
- mail compose hook
- multi-company workflow rule
- pre-init migration equivalent

Verify: `go test ./internal/workflow ./addons/oi_workflow_advance -run TestAdvancedWorkflow` exits 0.

### Step 6: Build delegation

From `oi_delegation` inventory, implement:
- delegation request
- delegation lines
- date-bounded delegation
- allowed delegable groups on groups model
- employee/user integration
- dynamic ACL/rule/menu/mail delegation behavior
- delegated active group resolution through effective groups
- delegated mail CC expansion
- delegation cache invalidation
- sequence
- cron expiration/revocation
- approval workflow integration
- delegation mail templates

Verify: `go test ./internal/delegation ./addons/oi_delegation -run TestDelegation` exits 0.

### Step 7: Build login-as

From `oi_login_as` inventory, implement:
- guarded wizard/action
- controller route
- route equivalent to `/web/login_as/<user_id>`
- route equivalent to `/web/login_back`
- guarded debug-only route equivalent to `/web/become/debug`, only if explicitly enabled
- session switch
- return-to-admin flow
- portal user support
- group-based option
- visible impersonation banner/context
- immutable audit log

Security rules:
- only authorized administrators can impersonate
- password never exposed
- original actor retained in session
- all actions during impersonation record actor and effective user
- no impersonation of inactive/superuser unless explicit permission exists

Verify: `go test ./internal/impersonation ./addons/oi_login_as -run TestLoginAs` exits 0.

### Step 8: Add cross-module install tests

Install order:
1. `oi_base`
2. `oi_workflow`
3. `oi_workflow_advance`
4. `oi_delegation`
5. `oi_login_as`

Verify:
- each module installs alone where dependencies allow
- all modules install together
- workflow plus delegation approvals work
- login-as respects delegated rights

Command: `go test ./addons/oi_* -run TestInstallAllOI` exits 0.

## Test Plan

- Manifest dependency tests.
- Workflow transition tests.
- Conditional approval tests.
- Approval button visibility tests.
- Voting and committee tests.
- Server-action, email-action, and method-action button tests.
- Escalation cron tests.
- Advanced workflow node/transition tests.
- Multi-company workflow rule tests.
- Delegation overlap, expiry, revocation tests.
- Dynamic delegated ACL/rule/menu tests.
- Delegated active group resolution tests.
- Login-as denial, switch, return, audit tests.
- Multi-company isolation tests.

## Done Criteria

- OI modules are installable apps in the Go ERP.
- `go test ./...` exits 0.
- Frontend OI tests pass.
- A generated inventory report maps every local OI 18 model/view/data/security/controller file to implemented, intentionally omitted, or blocked.
- No OI static media or proprietary code is copied into the repo without written license/provenance decision.

## STOP Conditions

- Exact source copying is required.
- OI scripts require arbitrary Python evaluation.
- Delegation bypasses record rules without auditable actor/effective-user separation.
- Login-as cannot retain original actor identity.

## Maintenance Notes

Build standard document workflow plugins after the base OI engine is stable.
