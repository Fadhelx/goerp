# Implementation Plans

Generated by `/improve deep` on 2026-06-16.

Repository: `/Users/fadhelalqaidoom/Documents/gorp`

Source trees:
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo19/odoo`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo19/enterprise`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo18/odoo18-addons`
- `/Users/fadhelalqaidoom/Desktop/odoo/odoo17/odoo17-addons`

Current repository state: empty git repository, no commits, no tracked files.

Rule: these plans target a Go reimplementation and feature-parity port. Do not paste, copy, or mechanically translate proprietary Enterprise or OI code/assets unless the operator has made a written license/provenance decision for that specific file set.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bootstrap Go ERP repository | P1 | M | - | DONE |
| 002 | Build base kernel, registry, ORM, modules, data loader | P1 | L | 001 | DONE |
| 003 | Build security, users, groups, ACLs, record rules | P1 | L | 002 | DONE |
| 004 | Build views, actions, menus, web controllers, OWL-compatible web client, themes | P1 | L | 002, 003 | DONE |
| 005 | Build automation, scheduled actions, mail, activities, notifications | P1 | L | 002, 003 | DONE |
| 006 | Build accounting module and enterprise accounting extension points | P1 | L | 002, 003, 005 | DONE |
| 007 | Build AI module | P2 | L | 002, 003, 004, 005 | DONE |
| 008 | Build OI base, workflow, workflow advance, delegation, login-as apps | P2 | L | 002, 003, 004, 005 | DONE |
| 009 | Build compatibility audit harness and source-inventory gates | P1 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED

## Dependency Notes

- 002 must precede all modules. Every feature depends on model metadata, module install state, data loading, migrations, and transactions.
- 003 must precede web, accounting, AI, and OI modules. Access control and record rules are part of every model operation.
- 004 must precede installable UI-heavy apps. Views, actions, menus, asset bundles, and OWL-compatible components are required for base UI and OI apps.
- 005 must precede workflow and AI. Workflow uses crons, mail templates, activities, notifications, and automation triggers.
- 006 uses base accounting from Odoo 19 and should not block OI workflow work except where accounting-specific workflows are added later.
- 008 should use Odoo 18 OI sources first, then compare Odoo 17 only for missing behavior.
- 009 runs from the start and remains active until all source inventories and parity checks are complete.

## Vetted Findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---------|----------|--------|--------|------|----------|
| 1 | Empty repository has no verification baseline | DX | No build, tests, lint, module install, or parity gate exists | M | LOW | `/Users/fadhelalqaidoom/Documents/gorp` contains only `.git` |
| 2 | Odoo compatibility must start with metadata kernel and ORM | Architecture | Later modules cannot be installable or secure without registry, model, field, recordset, transaction, module, and data semantics | L | HIGH | Odoo 19 local source: `odoo/odoo/addons/base`, docs: ORM recordsets and data-driven modules |
| 3 | Security is core behavior, not an add-on | Security | ACLs, record rules, field groups, menu visibility, and sudo affect every model and route | L | HIGH | Odoo 19 docs: access rights and record rules; local `base` security/data files |
| 4 | OI workflow modules are Odoo 18/17 OPL sources, not Odoo 19 modules | Migration | Need feature-parity port and version gap analysis before implementation | L | MED | Local `odoo18/odoo18-addons/oi_workflow*`, `oi_delegation`, `oi_login_as`; manifests show OPL-1 and Odoo 18 versions |
| 5 | AI and enterprise accounting require isolated clean-room extension points | Security | Provider keys, LLM tools, accounting reports, and enterprise features can leak data or lock the architecture | L | MED | Local `odoo19/enterprise/ai`, `odoo19/odoo/addons/account`, `odoo19/enterprise/account_*` |

## Direction Findings

- Build a Go platform with Odoo-shaped semantics, not a Python compatibility layer. Evidence: Odoo core relies on dynamic Python inheritance and recordsets; Go needs explicit metadata builders and generated/registered model definitions.
- Treat local Odoo 19 and OI 18/17 trees as behavior inventories and test-fixture sources. Feature parity should be proven by inventory coverage and tests, not by textual code copying.
- Keep Enterprise-like theme and Enterprise feature modules separate. The base platform should run without proprietary enterprise modules, then install compatible extension apps where licensing and source access permit.

## Agent Lanes

| Lane | Agent | Status | Output Used |
|------|-------|--------|-------------|
| Go base kernel | Laplace | DONE | Registry, ORM, module/data/security architecture |
| OWL/web/theme | Aristotle | DONE | OWL-compatible API, QWeb compiler, assets, themes |
| Security | Bernoulli | DONE | Users, groups, ACLs, record rules, sessions, audit |
| Automation/mail | Meitner | DONE | Cron, server actions, mail queue, notifications |
| Accounting | Copernicus | DONE | Accounting scope, chart/journal/tax/access tests |
| AI | Curie | DONE | Provider, prompts, tools, RAG, audit, permissions |
| OI modules | Linnaeus | DONE | Initial OI app boundaries before local path was known |
| Odoo 19 local base | Kierkegaard | DONE | Registry, ORM, module loader, security, web, automation, mail inventory |
| Odoo 19 local AI/accounting | Hooke | DONE | Accounting/enterprise accounting and AI inventory |
| OI local source | Poincare | DONE | OI base/workflow/advanced workflow/delegation/login-as inventory |
| Odoo 19 active_test source audit | Boole the 5th | DONE | `_search` active/x_active semantics, web/read_group/name_search/export implications |
| Local active_test gap audit | Einstein the 5th | DONE | HTTP context propagation, registry snapshot, scheduler, AI tool, x2many follow-ups |

## Recent Parity Slices

- 2026-06-19: DONE. Extended persistent mail queue delivery: SMTP fallback now resolves `ir.mail_server`, queue messages carry From/Reply-To/custom headers and attachments, SMTP emits multipart MIME payloads, failure types distinguish SMTP/invalid-recipient cases, and server-action email hooks persist template/request queue fields.
- 2026-06-19: DONE. Added bounded persistent `mail.mail` queue parity: expanded queue-related mail schema, due outgoing queue processing, `send`/`process_email_queue`/`action_retry`/`cancel` call_kw methods, sent/exception notification updates, runtime named queue actions, and source-shaped mail queue cron data.
- 2026-06-19: DONE. Added Odoo 19 mail-state server-action warning parity for negative activity deadlines, mail template model mismatch, transient target models, mail-thread/activity model requirements, and generic follower/activity-user relation chains; persisted `ir.model` mail metadata flags now drive registry warnings.
- 2026-06-19: DONE. Aligned accounting portal invoice record rules with Odoo 19: portal users can read posted customer/vendor invoices and refunds for their commercial partner hierarchy, draft/cancelled moves and receipts stay hidden, and move-line checks use denormalized local `parent_state`/`move_type`/`partner_id` fields.
- 2026-06-19: DONE. Implemented Odoo-style `active_test` filtering for Go record searches: default searches hide inactive rows, `context.active_test=false` includes them, explicit `active`/`x_active` domains suppress the implicit filter, and `x_active` is used only when no `active` field exists.
- 2026-06-19: DONE. Propagated `active_test` through HTTP `search`, `search_count`, `search_read`, `name_search`, `web_search_read`, `read_group`, export download, and `export_data`; AI search/read_group tool calls can pass `active_test`; loader, scheduler, workflow, and runtime registry snapshots load inactive rows where disabled records must remain addressable.
- 2026-06-19: DONE. Finished x2many relation-field `active_test` parity for `web_read`: field context takes precedence over env context for membership, default many2many membership is active-filtered and comodel-ordered, explicit web_read order uses the selected relation set, and specification context affects child reads without changing current membership.
- 2026-06-19: DONE. Matched Odoo `search_read` context scoping by stripping `active_test` before read formatting while preserving it for the primary search and leaving `web_search_read` unchanged.
- 2026-06-19: DONE. Added Odoo-shaped mail activity UI data methods: `mail.activity.activity_format`, `mail.activity.get_activity_data`, direct activity reschedule/cancel buttons, and `mail.activity.mixin.activity_reschedule` call_kw dispatch; included activity type template metadata and active/done aggregate behavior.

## Findings Considered And Rejected

- Implement directly in this turn: rejected. `/improve deep` was explicitly requested, and its workflow only permits plans under `plans/`.
- Full source read of all `/Users/fadhelalqaidoom/Desktop/odoo`: rejected for this turn. The tree is 26G and 291515 files. Plans include a source-inventory gate to make complete targeted coverage machine-checkable.
